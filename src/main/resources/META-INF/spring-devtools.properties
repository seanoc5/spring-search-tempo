# DevTools configuration
# Prevent restarts during batch job execution which can corrupt transactions

# Add additional paths to watch (none needed currently)
# restart.include.projectcommon=/my-project-common.*\.jar

# Exclude paths from triggering restart
# The /tmp directory is often used by batch jobs for temp files
#restart.exclude.tmp=/tmp/**
