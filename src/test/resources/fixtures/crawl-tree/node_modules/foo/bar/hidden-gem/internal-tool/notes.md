# Internal Tool Notes

This markdown file is a "hidden gem" — high-signal content buried under
node_modules. The folder audit / hidden-gem detection logic (issue #104)
should surface this file even though its parent tree is normally SKIP'd.

Useful keywords: deployment runbook, on-call rotation, escalation.
